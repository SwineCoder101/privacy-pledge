## Getting Started

First, run the development server:

```bash
npm run dev
# or
yarn dev
# or
pnpm dev
```

--

build static
`npm run build` without type:module

deploy static github pages
`npm run deploy`

